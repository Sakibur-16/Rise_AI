fastapi
uvicorn[standard]
python-dotenv
pydantic

# ── AI providers — install only what you need ──────────────────────────────
# Anthropic (Claude)
anthropic

# OpenAI (GPT), also used by Groq / Mistral / Together (OpenAI-compatible)
openai

# Google Gemini
# google-generativeai
